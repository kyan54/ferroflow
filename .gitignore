# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust workspace (cargo puts target/ at the workspace root, not src-tauri/)
/target/

# Local dev convenience: downloaded sing-box binary for manual testing
# (core-manager resolves it via FERROFLOW_SINGBOX_PATH or this path; never
# bundled/committed — see crates/core-manager/src/lib.rs::locate_binary).
/.dev-bin/

# Fetched dev/build dependency, same reasoning as /.dev-bin/ above: the
# SagerNet/sing-box-dashboard static build, downloaded by
# `npm run fetch:dashboard` (scripts/fetch-dashboard.mjs) into
# src-tauri/resources/dashboard/. Large, third-party, and reproducible from
# that script -- never committed.
/src-tauri/resources/dashboard/

# Built by `npm run build:helper` (scripts/build-helper.mjs) into
# src-tauri/resources/helper/ -- the current platform's privileged-helper
# binary, staged there so `bundle.resources` can package it. Platform-
# specific and reproducible from source, so never committed (same reasoning
# as /src-tauri/resources/dashboard/ above).
/src-tauri/resources/helper/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
